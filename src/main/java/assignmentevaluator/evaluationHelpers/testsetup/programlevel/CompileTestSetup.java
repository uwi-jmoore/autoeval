package assignmentevaluator.evaluationHelpers.testsetup.programlevel;

/**
 * This class provides a setup service for compile tests at the program level.
 * It extends the {@link ProgramTestSetup} class and can be used to configure 
 * the necessary setup details for compile tests, such as the assignment directory.
 */
public class CompileTestSetup extends ProgramTestSetup {

    /**
     * Constructor for the CompileTestSetup class.
     * It calls the superclass constructor to initialize the setup map and 
     * prepares the map for setting up compile tests.
     */
    public CompileTestSetup(){
        super();
    }
}
